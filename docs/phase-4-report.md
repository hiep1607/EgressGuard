# Phase 4 implementation report

Date: 2026-08-11. Status vocabulary: `Unit tested`, `Integration tested`, `Verified`, `Not verified`, and `Blocked` are intentionally distinct.

| Area | Status | Evidence |
|---|---|---|
| Pure temporal correlation | Unit tested | Exact `(PID, ProcessStartTime)`, configurable -30/+5 second window, out-of-order input, a dedupe hard bound equal to event capacity, safe same-key eviction, multiple files/flows, evidence cap, overflow counter, retention, and storage exclusion. |
| Windows ETW sensor and pre-flow promotion | Verified | The elevated TraceEvent collector retained raw file events before a network flow was known, then synchronously promoted the synthetic `.egfixture` read when the network snapshot supplied the exact `(PID, ProcessStartTime)`. The raw buffer is globally capped at 4,096 events, 256 per PID, with 35-second retention; system installation paths are rejected by cheap checks. No File I/O callback performs process lookup. |
| PID reuse safety | Unit and integration tested | Generation replacement is keyed by exact `(PID, ProcessStartTime)`. Events before the new generation start are discarded, stale stops cannot remove a newer generation, and raw promotion tests exercise the same sensor interest path. Process metadata enrichment failures no longer discard the exact process identity. |
| ETW lifecycle | Verified | The Administrator lifecycle integration completed 10 start/stop cycles, including a cancellation race. Every cycle stopped the exact owned session and removed its marker; foreign/shared sessions were untouched. Final active EgressGuard ETW session count was zero without success-path prefix cleanup. |
| Service degradation | Integration tested | Disabled and controlled AccessDenied sensors leave the network coordinator running and shut down cleanly. |
| SQLite v3 | Integration tested | Idempotent migration, parameterized write/read of every already-bounded item, 10 flows × 20 correlations without later-flow truncation, duplicate-ID idempotence, bounded query, indexes, retention, foreign key, and Clear history deletion. |
| IPC | Unit tested | Optional status fields and bounded `GetFileCorrelations`; legacy status without Phase 4 fields deserializes. Existing framing remains capped at 1 MiB. |
| UI | Verified at 125%; 100%/150% Not verified | UI Automation and screenshots covered all six tabs, selected/empty Related file activity, long values, scrollbar, disabled/degraded presentation, 900×560, maximize/minimize/normal, tray lifecycle, and UI-close/service-survival. No clipping, overlap, or contrast defect was observed. The live sensor degraded before a stable `Running` screenshot; that presentation and tooltip appearance still require manual confirmation. |
| Simulator/service IPC | Verified | The real service correlated the synthetic `.egfixture` read that occurred before the first loopback flow (`TimeDeltaSeconds < 0` in the -30/+5 window), returned exact process identity through IPC, persisted only protected/redacted identifiers, and the test server recorded zero transmitted bytes. Fixtures, markers and sessions were cleaned. |
| Performance | Failed / not merge-ready | Fresh same-machine 45-second phases on 12 logical CPUs measured service CPU: disabled baseline 3.938% average (8.854% peak), enabled idle 5.414% (12.370% peak), normal pre-flow 3.166% (4.818% peak), and stress/churn 6.152% (14.453% peak). UI CPU remained responsive (0.055–2.532% average). Service RAM stayed bounded at 65.9–91.8 MB; UI RAM at 159.3–179.5 MB. The normal <3% service budget is not met, so Phase 4 is not complete. Hard bounds remain enforced (raw 4,096/global, 256/PID, pending 4,096, interest cache 4,096); dropped-count coalescing and exact final flush were verified by dedicated regression/IPC status tests, but were not captured as a separate performance-phase counter. |
| Production signing/Application Control | Blocked | Unchanged from Phases 1–3.5; this is not a production release. |

## Architecture and privacy

The sensor captures a timestamp, PID, process name, operation and normalized path in a short-lived bounded raw buffer, but never opens file content. Once a network snapshot supplies an exact process identity, only matching events at or after that process generation start are promoted to `FileActivity`; promotion is synchronous for pre-flow correlation and duplicate-safe. The deterministic engine selects related events and persists only a salted path hash, redacted display identifier, extension, timestamps, delta, confidence and reason. No packet content, HTTPS plaintext, credential, clipboard, keystroke, cookie/token, or external telemetry is collected.

The feature is observe-only and disabled by default. File evidence never raises risk, creates a firewall rule, or activates enforcement. ETW callbacks do only cheap validation and bounded `TryWrite`; they never call `Process.GetProcessById` or `Process.StartTime`. The recent raw buffer has global/per-PID hard bounds and 35-second retention; the exact process-interest cache is hard-bounded with a one-minute TTL, and PID reuse is generation-safe. Status notifications coalesce dropped-count changes to at most about once per second and flush the exact final count on stop. Correlations are capped at 20 per flow and stored under the normal 30-day local retention. Persistence writes the complete already-bounded correlation enumeration instead of silently taking only its first 100 items. Users can remove evidence with Clear history.

## Provider, ownership, and dependency

The Windows sensor uses `Microsoft.Diagnostics.Tracing.TraceEvent` 3.2.5, owned by Microsoft, because it provides maintained kernel File I/O ETW parsing/session control without a custom unsafe P/Invoke layer. A session is named `EgressGuard.FileActivity.v2-{nonce}`. Its marker directory has protected ACLs for the controller identity, `SYSTEM`, and Administrators. The marker must exactly match version, nonce, session, PID, and start time; a live owner blocks a second controller, while a dead verified owner permits stopping only that exact session. Missing/invalid markers never authorize prefix cleanup. Local Administrators remain inside the trust boundary.

## Limitations and remaining acceptance

- Temporal correlation does not prove upload or identify transmitted contents.
- Overflow, provider variation, permission failure, late identity lookup, or very short processes can cause missed events.
- File metadata is sensitive even after redaction; evidence stays local and bounded.
- Real ETW pre-flow promotion, PID-reuse generation handling, 10-cycle lifecycle cleanup, crash/reclaim, service IPC, zero-byte loopback, raw-path redaction, and the 125% UI integration are `Verified`. Phase 4 UI at 100%/150%, a stable direct `Running` sensor presentation, and tooltip appearance remain `Not verified`.
- Performance is `Failed` for this head because the fresh post-change normal service CPU average was 3.166% and stress was 6.152%. The earlier 17.2% result and earlier focused table are historical evidence from different source and must not be presented as current acceptance; no merge or Phase 4 completion should be recommended until the budget is met without weakening assertions.
